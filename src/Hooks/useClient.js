import React from 'react'
import {useQuery,
        useMutation,
        useQueryClient,
        QueryClient,
        QueryClientProvider} from 'react-query'

const useClient = () => {
  const queryClient = useQueryClient
  const query = 
}